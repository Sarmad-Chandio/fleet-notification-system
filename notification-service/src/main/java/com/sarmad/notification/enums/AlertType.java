package com.sarmad.notification.enums;

public enum AlertType {
    VEHICLE,        // Alerts related to a vehicle (maintenance, breakdown, speeding, etc.)
    DRIVER,         // Alerts related to a driver (violation, fatigue, login/logout, etc.)
    EMERGENCY,
    OTHERS
}
